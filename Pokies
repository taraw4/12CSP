# Pokies

# Get randomiser
import random

# Create symbols list
symbols = ["7", "$", "*", "#"]

# Empty Grid
grid = []

#Generate grid
for row in range(3):
    random.shuffle(symbols)
    print(symbols)
    
